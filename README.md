# AplicacionWeb

## Instrucciones para subir a GitHub

Para subir todo el código fuente a tu repositorio remoto y poder descargarlo en otro navegador o dispositivo, abre tu terminal en la carpeta del proyecto y ejecuta:

```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/salasCalacuta/AplicacionWeb.git
git push -u origin main
```
